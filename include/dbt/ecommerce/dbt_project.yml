name: ecommerce
version: '1.0.0'
profile: ecommerce

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  ecommerce:
    silver:
      +materialized: view
      +schema: silver
    gold:
      +materialized: table
      +schema: gold
      bi:
        +materialized: table
        +schema: gold_bi
